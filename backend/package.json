{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"  // Comando que o Render usará para iniciar o app
  },
  "dependencies": {
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "cors": "^2.8.5",
    "socket.io": "^4.0.0"  // Dependência para chat em tempo real
  },
  "engines": {
    "node": "20.x"  // Especifica a versão do Node
  }
}

